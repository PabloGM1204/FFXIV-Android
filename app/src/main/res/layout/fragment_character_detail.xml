<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/topAppBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="@android:drawable/ic_menu_revert" />


    <!-- ID -->
    <TextView
        android:id="@+id/label_character_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ID: "
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/topAppBar" />

    <TextView
        android:id="@+id/character_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/label_character_id"
        app:layout_constraintTop_toTopOf="@id/label_character_id"
        app:layout_constraintBottom_toBottomOf="@id/label_character_id"
        tools:text="001"/>

    <!-- Nombre -->
    <TextView
        android:id="@+id/label_character_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nombre: "
        app:layout_constraintTop_toBottomOf="@id/label_character_id"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="8dp"/>

    <TextView
        android:id="@+id/character_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@id/label_character_name"
        app:layout_constraintStart_toEndOf="@id/label_character_name"
        app:layout_constraintTop_toTopOf="@id/label_character_name"
        tools:text="Lista" />

    <!-- Botón de Eliminación -->
    <ImageButton
        android:id="@+id/btn_delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="16dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Eliminar Lista"
        android:src="@android:drawable/ic_menu_delete"
        android:tint="#FF0000"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/label_character_name" />

    <!-- Agrega este espacio adicional si es necesario -->
    <Space
        android:id="@+id/space"
        android:layout_width="8dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/label_character_name"
        app:layout_constraintStart_toEndOf="@+id/btn_delete"
        app:layout_constraintEnd_toStartOf="@+id/label_character_name"
        />



</androidx.constraintlayout.widget.ConstraintLayout>